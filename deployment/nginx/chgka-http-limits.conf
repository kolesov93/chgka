# Install in /etc/nginx/conf.d/. These zones are referenced only by CHGKA's
# Socket.IO location and do not alter existing sites.
limit_conn_zone $binary_remote_addr zone=chgka_ws_connections:1m;
limit_req_zone $binary_remote_addr zone=chgka_ws_handshakes:1m rate=3r/s;
